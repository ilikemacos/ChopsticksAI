import SwiftUI

/// Legacy entry point kept for Xcode grouping; tabbed surface lives in `TabbedExpandedView`.
struct ExpandedIslandContent: View {
    var body: some View {
        TabbedExpandedView()
    }
}
