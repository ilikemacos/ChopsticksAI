# Legacy `Sources/`

This folder is a **stale snapshot** of an older ChopsticksAI layout. Do not compile it.

Canonical sources:

- `macos-app/` — SwiftUI app (`CloudAuth.swift`, not `SupabaseCloud.swift`)
- `engine/` — offline KB
- `shared/` — auto-update
- `server/` — API snapshot for the GitHub mirror
- `cli/` — `csai`
